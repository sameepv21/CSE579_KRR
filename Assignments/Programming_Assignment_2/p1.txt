% Constants
const max_blocks_on_table = 3.

% Load the original blocks.lp file
:- include('blocks.lp').

% Define the new assumption
#const num_grippers = 1.

% Add a new constraint to reflect the assumption about the table size
1{on(B,table,T)}1 :- block(B), T = 0..m, #count{X: on(X,table,T)} < max_blocks_on_table.

% Test the program with the scenario in blocks-scenario.lp
#show move/3.
:- include('blocks-scenario.lp').